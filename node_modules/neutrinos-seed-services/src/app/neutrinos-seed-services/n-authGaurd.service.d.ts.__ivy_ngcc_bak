import { Router, CanActivate } from '@angular/router';
import { NLoginService } from './n-login.service';
export declare class NAuthGuardService implements CanActivate {
    private nLoginService;
    private router;
    constructor(nLoginService: NLoginService, router: Router);
    canActivate(): Promise<boolean>;
}
